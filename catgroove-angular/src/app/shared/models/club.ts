import Staff from "./staff";

export default class Club {
  image?: string;
  key?: string | null;
  name?: string;
  description?: string;
  datacenter?: string;
  server?: string;
  area?: string;
  ward?: string;
  plot?: string;
}